# Legacy v0.1 examples

Single-file examples in the superseded v0.1 format (see `spec/v0.1-draft.md`).
Kept for reference; the v0.2 CLI rejects them. Current examples live in the
sibling directories as `.agents/` layouts.
