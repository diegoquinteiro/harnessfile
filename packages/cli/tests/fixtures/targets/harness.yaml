harnessfile: "0.2"
name: targets-harness

runtimes:
  claude:
    protocol: claude-code/v1
  codex:
    protocol: codex-app-server/v1

steps:
  work:
    agent: worker

targets:
  multica:
    provider: multica/v1
    owns: [model, runtime, thinking-level, concurrency, env, mcp]
  claude-code:
    provider: claude-code/v1
  codex:
    provider: codex/v1
  github:
    provider: github-agent-hq/v1
  bad-owns:
    provider: multica/v1
    owns: [model, flux-capacitor]
